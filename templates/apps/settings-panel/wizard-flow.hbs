<section class="tab hm-dialog-tab standard-form {{#if tabs.wizard-flow.active}}active{{/if}}" data-group="primary"
  data-tab="wizard-flow">
  {{#each wizardFlow.groups}}
    {{#if this.label}}<h3 class="hm-sp-group-title">{{localize this.label}}</h3>{{/if}}
    {{#each this.settings}}{{> hmSettingsPanelField}}{{/each}}
  {{/each}}
</section>
