**Changes**
- Removed the `recommends` list from module.json, so Foundry's dependency dialog no longer offers to enable Spell Book, Dice So Nice, Tokenizer 2, or Calendaria when you turn on Hero Mancer in Manage Modules.
- The integrations with those modules still work when installed; only the automatic companion-module suggestions in the dependency dialog are gone.
